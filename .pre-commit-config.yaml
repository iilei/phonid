repos:
  - repo: https://github.com/editorconfig-checker/editorconfig-checker.python
    rev: 11ea70506fd878d26129b2b87746fd1a1f6b561b  # frozen: 3.6.0
    hooks:
      - id: editorconfig-checker
        alias: ec
        exclude: '\.go$'  # Go formatting is handled by gofmt and editorconfig-checker yields false positives

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 3e8a8703264a2f4a69428a0aa4dcb512790b2c8c  # frozen: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: '^(internal/provider/.*\.md)|docs/.*$'
      - id: end-of-file-fixer
        exclude: '^(internal/provider/.*\.md)|docs/.*$'
      - id: check-yaml
      - id: check-added-large-files
      - id: mixed-line-ending
        args: ['--fix=lf']

  - repo: https://github.com/golangci/golangci-lint
    rev: e2e40021c9007020676c93680a36e3ab06c6cd33  # frozen: v2.8.0
    hooks:
      - id: golangci-lint-full

  - repo: local
    hooks:
      - id: goimports
        name: goimports
        entry: bash -c 'goimports -w $(find . -type f -name "*.go" -not -path "./vendor/*")'
        language: system
        types: [go]
        pass_filenames: false

      - id: go-fmt
        name: go fmt
        entry: bash -c 'output=$(go fmt ./...); if [ -n "$output" ]; then echo "Files not formatted:"; echo "$output"; exit 1; fi'
        language: system
        types: [go]
        pass_filenames: false

      - id: go-vet
        name: go vet
        entry: bash -c 'go vet ./... || exit 0'
        language: system
        types: [go]
        pass_filenames: false

      - id: go-test
        name: go test
        entry: go test -short ./...
        language: system
        types: [go]
        pass_filenames: false

      # pre: go install github.com/princjef/gomarkdoc/cmd/gomarkdoc@latest
      - id: go-doc-generate
        name: generate Go documentation
        entry: bash -c 'gomarkdoc ./pkg/... > docs/API.md'
        language: system
        pass_filenames: false
        files: 'pkg/.*\.go$'
